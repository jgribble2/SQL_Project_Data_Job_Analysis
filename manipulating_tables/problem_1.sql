-- create a table named "data_science jobs"
-- include the following columns: "job_id" (integer and primary key), "job_title" (text), "company_name" (text), and "post_date" (date)

CREATE TABLE data_science_jobs (
    job_id INT,
    job_title TEXT,
    company_name TEXT,
    posted_date DATE
);